meta {
  name: users-list
  type: http
  seq: 1
}

get {
  url: {{host}}/admin/users?status=Active&role=Admin
  body: none
  auth: inherit
}

params:query {
  status: Active
  role: Admin
  ~search: webdev
}

settings {
  encodeUrl: true
  timeout: 0
}
